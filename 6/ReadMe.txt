Unit 6 Project - Brian Steuble

This program converts hack assembly instructions into binary machine code.

To run the program, open a terminal session within the project 6 directory, compile using the
'make' command. Then type ./main [name of file to assemble].

The program will create a new file with the hack code translated into binary.

Main is driver program for this project.